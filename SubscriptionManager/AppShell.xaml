<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SubscriptionManager.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SubscriptionManager.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="SubscriptionManager">

    <ShellContent
        Title="Subskrypcje"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <!--<ShellContent
        Title="Subskrypcja"
        ContentTemplate="{DataTemplate local:SubscriptionDetailPage}" 
        Route="SubscriptionDetailPage" />-->

</Shell>
